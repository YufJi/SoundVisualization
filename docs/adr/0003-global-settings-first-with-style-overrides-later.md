# Make the first settings model global-first

SoundViz exposes Visualization Settings as Global Parameters in the first settings release. Style Override remains part of the domain model as an extension point, but does not receive user-facing controls yet. This keeps the first settings window understandable while leaving room for per-style tuning after the core parameters stabilize.
