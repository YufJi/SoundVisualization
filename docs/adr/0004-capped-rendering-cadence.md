# Cap menu-bar rendering by activity and power state

SoundViz uses 30 fps as the standard Rendering Cadence and may allow 60 fps as High Cadence. During Low-distraction Baseline or low-power conditions, rendering is capped at 15 fps. This keeps the menu bar responsive without making an always-visible utility continuously expensive.
