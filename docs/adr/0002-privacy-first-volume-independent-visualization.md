# Keep visualization privacy-first and independent of system volume

SoundViz derives visual motion from spectral audio content, not the system volume setting. It processes audio only in memory and does not record, persist, upload, or play captured audio. This preserves a clear privacy boundary and prevents the visualizer from changing meaning when the user merely adjusts output loudness, at the cost of not exposing a simple volume meter.
